# Python requirements for CW protocol test implementation

# Optional: for audio sidetone
pyaudio>=0.2.11

# For UDP socket communication (built-in)
# No additional packages required

# Testing and development
# numpy is required if pyaudio is used
numpy>=1.19.0

# For USB key interface
pyserial>=3.5

USB-Serial Adapter (DB9 or terminal block)
├── CTS (pin 8)  ──── Dit paddle (left)
├── DSR (pin 6)  ──── Dah paddle (right)
└── GND (pin 5)  ──── Common ground